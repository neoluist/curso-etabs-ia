# Cajas del SERVIDOR de la app ETABS + IA (Python 3.12)
# Versiones fijadas = las mismas validadas en el curso.
fastapi==0.137.1
uvicorn==0.49.0
comtypes==1.4.16
openpyxl==3.1.5
# openseespy se instala en la recta final del curso (verificacion cruzada);
# el servidor funciona sin el y la pestana OpenSees avisara que falta.
